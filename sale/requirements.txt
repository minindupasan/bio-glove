# SALE Component 3 — Dependencies
# Requires Python 3.11+ (mediapipe/jax use match statements — Python 3.10+ syntax)
# Setup: python3.11 -m venv venv && source venv/bin/activate && pip install -r requirements.txt

# Core ML / Data
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11

# Visualisation (training pipeline)
matplotlib>=3.7
seaborn>=0.12

# Computer Vision / Face Analysis
opencv-python>=4.8
mediapipe==0.10.18
deepface>=0.0.79
tf-keras                          # required by newer deepface versions
protobuf>=4.25.3                  # mediapipe 0.10.18+ requires this; fixes GetPrototype bug

# TensorFlow — Apple Silicon (M1/M2/M3)
# On Mac ARM: pip install tensorflow-macos tensorflow-metal
# On Intel / Linux / Windows: pip install tensorflow
# deepface will pull tensorflow automatically, but pin here for reproducibility
tensorflow-macos; sys_platform == "darwin" and platform_machine == "arm64"
tensorflow; sys_platform != "darwin" or platform_machine != "arm64"

# HTTP / Networking
requests>=2.31                    # used by deepface internals

# Serial (IoT glove)
pyserial>=3.5

# Firebase — no extra package needed (uses urllib, credentials in config.py)
